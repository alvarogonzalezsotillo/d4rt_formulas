name: Create release from tag

on:
  push:
    tags:
      - 'release-*'

jobs:
  create_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # needed to create releases
    steps:
      - name: Environment Variables
        run: env

      - name: Checkout
        uses: actions/checkout@v4

      - name: Do release on my own, actions are not nerd enough, maybe works even in other CI/CD systems
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./release.sh
